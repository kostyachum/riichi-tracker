DJANGO_DEBUG=True
DJANGO_SECRET_KEY=dev-secret-change-me
DJANGO_ALLOWED_HOSTS=localhost,127.0.0.1

# No DB vars needed (SQLite by default)

# --- Production reverse proxy (Traefik) ---
# Public hostname for the app (used by Traefik + Django)
APP_HOST=your.domain
